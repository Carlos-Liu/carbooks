namespace CarBooks.Application.Shared;

/// <summary>
/// Marker implemented by every application service so the Autofac module can register them by
/// convention instead of listing each one.
/// </summary>
public interface IApplicationService
{
}
